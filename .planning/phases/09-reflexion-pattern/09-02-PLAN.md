---
phase: 09-reflexion-pattern
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/agent.ts
  - frontend/src/types/sse.ts
  - frontend/src/composables/useSSEStream.ts
  - frontend/src/App.vue
autonomous: true
requirements: [UI-11]
user_setup: []

must_haves:
  truths:
    - "ReflexionAttemptEvent TS interface has (ts, round, answer) (per D-03)"
    - "ReflexionEvaluateEvent TS interface has (ts, round, score, feedback) (per D-03)"
    - "ReflexionReflectEvent TS interface has (ts, round, reflection) (per D-03)"
    - "AgentEvent union type and AgentEventName include all 3 Reflexion events (15 total) (per D-03)"
    - "useSSEStream routes ReflexionAttemptEvent/ReflexionEvaluateEvent/ReflexionReflectEvent to onReflexionAttempt/onReflexionEvaluate/onReflexionReflect callbacks"
    - "App.vue adds 3 Reflexion callbacks appending events to agentEvents on every submit"
    - "Reflexion events are NOT appended to reasoningText (content carried by ReflexionAttemptEvent.answer, not ReasoningEvent stream)"
    - "PatternSelector enables reflexion mode (green '已上线' tag)"
    - "patternDetails.ts has reflexion entry with coreIdea + scenarios filled (per UI-SPEC copywriting contract)"
    - "PatternDescriptionCard shows 'Reflexion 反思迭代' title"
  artifacts:
    - path: frontend/src/types/agent.ts
      provides: ReflexionAttemptEvent/ReflexionEvaluateEvent/ReflexionReflectEvent interfaces + updated AgentEvent union
      min_lines: 210
    - path: frontend/src/types/sse.ts
      provides: 3 new AgentEventName literals
      min_lines: 30
    - path: frontend/src/composables/useSSEStream.ts
      provides: onReflexionAttempt/onReflexionEvaluate/onReflexionReflect routing
      min_lines: 200
    - path: frontend/src/App.vue
      provides: 3 Reflexion event callback handlers
      min_lines: 200
    - path: frontend/src/constants/patternDetails.ts
      provides: reflexion pattern details (coreIdea + scenarios)
      min_lines: 90
  key_links:
    - from: useSSEStream.ts
      to: types/agent.ts
      via: import of ReflexionAttemptEvent, ReflexionEvaluateEvent, ReflexionReflectEvent
      pattern: "import.*Reflexion(Attempt|Evaluate|Reflect)Event"
    - from: App.vue
      to: useSSEStream.ts
      via: onReflexionAttempt/onReflexionEvaluate/onReflexionReflect in startSSEStream options
      pattern: "onReflexion(Attempt|Evaluate|Reflect)"
---

<objective>
实现 Reflexion 模式前端数据层与启用：TypeScript 类型镜像（3 个新接口 + 联合类型）、AgentEventName 扩展、useSSEStream 事件路由、App.vue 回调、PatternSelector 启用、patternDetails 填充。

Purpose: 前端接收并路由 Reflexion 的 3 类新事件，用户在 PatternSelector 中点选 reflexion 模式后可提交问题，事件进入 agentEvents 列表供 ReasoningPanel（Plan 03）按轮次分组渲染。

Output:
- types/agent.ts（新增 3 个 Reflexion 接口 + AgentEvent 联合类型 15 个）
- types/sse.ts（AgentEventName 添加 3 个事件名）
- useSSEStream.ts（新增 onReflexionAttempt/onReflexionEvaluate/onReflexionReflect 路由）
- App.vue（新增 3 个 Reflexion 回调 + onReasoning 判断）
- PatternSelector.vue（reflexion 模式启用，绿色"已上线"）
- patternDetails.ts（reflexion 条目填充 coreIdea + scenarios）
- PatternDescriptionCard.vue（card-title 支持 reflexion）

注意（与 09-UI-SPEC.md 的差异处理）：UI-SPEC §Copywriting Contract 声明了 "提前停止说明" 和 "最终答案来源说明" 等推理面板内文案 —— 这些属 ReasoningPanel（Plan 03）渲染范围，本 Plan 不涉及。本 Plan 只处理类型、路由、回调、模式启用、模式详情。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-reflexion-pattern/09-CONTEXT.md
@.planning/phases/09-reflexion-pattern/09-UI-SPEC.md
@.planning/phases/09-reflexion-pattern/09-PATTERNS.md

<interfaces>
<!-- Existing frontend interfaces the executor needs. Extracted from codebase. -->

From frontend/src/types/agent.ts (current, 12 types):
```typescript
export type AgentEvent =
  | ReasoningEvent
  | ToolCallEvent
  | ToolResultEvent
  | SubQuestionEvent
  | SubAnswerEvent
  | PlanEvent
  | StepStartEvent
  | StepCompleteEvent
  | TotNodeEvent        // Phase 8 新增
  | TotPruneEvent       // Phase 8 新增
  | FinalAnswerEvent
  | ErrorEvent
```

From frontend/src/types/sse.ts (current, 12 names):
```typescript
export type AgentEventName =
  | 'ReasoningEvent'
  | 'ToolCallEvent'
  | 'ToolResultEvent'
  | 'SubQuestionEvent'
  | 'SubAnswerEvent'
  | 'PlanEvent'
  | 'StepStartEvent'
  | 'StepCompleteEvent'
  | 'TotNodeEvent'       // Phase 8 新增
  | 'TotPruneEvent'      // Phase 8 新增
  | 'FinalAnswerEvent'
  | 'ErrorEvent'
```

From frontend/src/composables/useSSEStream.ts (current SSEStreamOptions):
```typescript
export interface SSEStreamOptions {
  onReasoning: (content: string, ev: ReasoningEvent) => void
  onFinal: (content: string, ev: FinalAnswerEvent) => void
  onError: (message: string, ev: ErrorEvent) => void
  onToolCall?: (ev: ToolCallEvent) => void
  onToolResult?: (ev: ToolResultEvent) => void
  onSubQuestion?: (ev: SubQuestionEvent) => void
  onSubAnswer?: (ev: SubAnswerEvent) => void
  onPlan?: (ev: PlanEvent) => void
  onStepStart?: (ev: StepStartEvent) => void
  onStepComplete?: (ev: StepCompleteEvent) => void
  // Phase 8 新增
  onTotNode?: (ev: TotNodeEvent) => void
  onTotPrune?: (ev: TotPruneEvent) => void
  signal?: AbortSignal
}
```

From frontend/src/App.vue (current submit() pattern):
```typescript
onTotNode: (ev) => {
  totTree.addNode(ev)
  agentEvents.value = [...agentEvents.value, ev]
},
onTotPrune: (ev) => {
  totTree.markPruned(ev)
  agentEvents.value = [...agentEvents.value, ev]
},
```

From frontend/src/components/PatternSelector.vue (current isPatternEnabled):
```typescript
function isPatternEnabled(patternId: string): boolean {
  return patternId === 'cot'
    || patternId === 'react'
    || patternId === 'selfAsk'
    || patternId === 'planExecute'
    || patternId === 'tot'  // Phase 8 新增
}
// PHASE_ONLINE_MAP 已有 reflexion: 9 -- 无需修改
// reflexion 已在 HARDCODED_PATTERNS_FALLBACK 中：{ id: 'reflexion', displayName: 'Reflexion 反思迭代', description: '犯错后自我纠错' }
```

From frontend/src/constants/patternDetails.ts (current PatternDetail 接口 + 空的 reflexion 条目):
```typescript
export interface PatternDetail {
  coreIdea: string
  scenarios: string
}
// reflexion: { coreIdea: '', scenarios: '' },  -- 需填充
```

From frontend/src/components/PatternDescriptionCard.vue (current card-title):
```vue
<div class="card-title">{{
    patternId === 'tot' ? 'Tree of Thoughts 树状思维' :
    patternId === 'planExecute' ? 'Plan-and-Execute 计划与执行' :
    patternId === 'selfAsk' ? 'Self-Ask 自问自答' :
    patternId === 'react' ? 'ReAct 推理+行动' :
    patternId === 'cot' ? 'CoT 思维链' : patternId
  }}</div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3 Reflexion TS interfaces + AgentEventName + useSSEStream routing</name>
  <files>
    frontend/src/types/agent.ts
    frontend/src/types/sse.ts
    frontend/src/composables/useSSEStream.ts
  </files>
  <read_first>
    frontend/src/types/agent.ts
    frontend/src/types/sse.ts
    frontend/src/composables/useSSEStream.ts
  </read_first>
  <action>
    **types/agent.ts (per D-03):**

    1. 更新文件顶部注释：12 -> 15 record subtypes。
    2. 在 TotPruneEvent 接口之后添加 3 个新接口：
    ```typescript
    /**
     * ReflexionAttemptEvent - mirrors backend com.agents.agent.core.ReflexionAttemptEvent.
     * Reflexion 每轮尝试的答案内容 (Phase 9 用).
     */
    export interface ReflexionAttemptEvent {
      ts: InstantString
      round: number
      answer: string
    }

    /**
     * ReflexionEvaluateEvent - mirrors backend com.agents.agent.core.ReflexionEvaluateEvent.
     * Reflexion 评估结果（分数 + 反馈）(Phase 9 用).
     */
    export interface ReflexionEvaluateEvent {
      ts: InstantString
      round: number
      score: number
      feedback: string
    }

    /**
     * ReflexionReflectEvent - mirrors backend com.agents.agent.core.ReflexionReflectEvent.
     * Reflexion 反思内容（下一轮改进依据）(Phase 9 用).
     */
    export interface ReflexionReflectEvent {
      ts: InstantString
      round: number
      reflection: string
    }
    ```

    3. AgentEvent 联合类型在 TotPruneEvent 之后添加：
    ```typescript
    | ReflexionAttemptEvent    // Phase 9 新增
    | ReflexionEvaluateEvent   // Phase 9 新增
    | ReflexionReflectEvent    // Phase 9 新增
    ```

    **types/sse.ts:** AgentEventName 在 'TotPruneEvent' 之后添加：
    ```typescript
    | 'ReflexionAttemptEvent'    // Phase 9 新增
    | 'ReflexionEvaluateEvent'   // Phase 9 新增
    | 'ReflexionReflectEvent'    // Phase 9 新增
    ```
    更新顶部注释（12 -> 15 个 event name）。

    **useSSEStream.ts (per 09-UI-SPEC §useSSEStream Refactor):**

    1. import 块添加 ReflexionAttemptEvent, ReflexionEvaluateEvent, ReflexionReflectEvent。
    2. SSEStreamOptions 在 onTotPrune 之后添加（更新 JSDoc：Phase 9 新增 3 个回调）：
    ```typescript
    // Phase 9 新增
    onReflexionAttempt?: (ev: ReflexionAttemptEvent) => void
    onReflexionEvaluate?: (ev: ReflexionEvaluateEvent) => void
    onReflexionReflect?: (ev: ReflexionReflectEvent) => void
    ```
    3. switch 语句 'TotPruneEvent' case 之后、default 分支之前添加：
    ```typescript
    case 'ReflexionAttemptEvent':
      options.onReflexionAttempt?.(data as ReflexionAttemptEvent)
      break
    case 'ReflexionEvaluateEvent':
      options.onReflexionEvaluate?.(data as ReflexionEvaluateEvent)
      break
    case 'ReflexionReflectEvent':
      options.onReflexionReflect?.(data as ReflexionReflectEvent)
      break
    ```
  </action>
  <verify>
    <automated>cd /Volumes/D/dev/workspace/AI/agents/frontend && npx vue-tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ReflexionAttemptEvent 接口有 ts/round/answer 字段，ReflexionEvaluateEvent 有 ts/round/score/feedback，ReflexionReflectEvent 有 ts/round/reflection
    - AgentEvent 联合类型包含 3 个 Reflexion 类型（共 15 个）
    - AgentEventName 包含 3 个 Reflexion 事件名字符串字面量（共 15 个）
    - SSEStreamOptions 有 onReflexionAttempt/onReflexionEvaluate/onReflexionReflect 可选回调
    - useSSEStream switch 路由 3 个新事件名到对应回调（可选链 ?. 调用）
    - vue-tsc --noEmit 零错误
  </acceptance_criteria>
  <done>types/agent.ts 含 3 个 Reflexion 接口且 AgentEvent 联合类型 15 个；types/sse.ts AgentEventName 15 个；useSSEStream 路由 3 个新事件；vue-tsc 零错误。</done>
</task>

<task type="auto">
  <name>Task 2: Add 3 Reflexion callbacks + onReasoning guard in App.vue</name>
  <files>
    frontend/src/App.vue
  </files>
  <read_first>
    frontend/src/App.vue
    frontend/src/composables/useSSEStream.ts
  </read_first>
  <action>
    **App.vue (per 09-UI-SPEC §App.vue Refactor):**

    1. import 类型扩展 -- 在现有 `import type { AgentRequest, AgentEvent } from '@/types/agent'` 中补充 Reflexion 类型。无需单独 import（AgentEvent 联合类型已包含），但如直接使用类型标注则需 import。

    2. submit() 中 startSSEStream options 的 onTotPrune 回调之后添加 3 个回调：
    ```typescript
    // Phase 9: Reflexion 事件回调 -- 全部经 agentEvents 传递，不追加 reasoningText
    onReflexionAttempt: (ev) => {
      agentEvents.value = [...agentEvents.value, ev]
    },
    onReflexionEvaluate: (ev) => {
      agentEvents.value = [...agentEvents.value, ev]
    },
    onReflexionReflect: (ev) => {
      agentEvents.value = [...agentEvents.value, ev]
    },
    ```

    3. onReasoning 回调添加模式判断（per UI-SPEC §App.vue Refactor 结尾说明）：Reflexion 的 Generator 阶段会产生 ReasoningEvent，但由于 Reflexion 的答案内容通过 ReflexionAttemptEvent.answer 承载（`.call().content()` 一次性返回），不会产生 ReasoningEvent。即使后端某天发射了 ReasoningEvent，也要求前端在 reflexion 模式下**不将 ReasoningEvent 追加到 reasoningText**。修改 onReasoning 回调：
    ```typescript
    onReasoning: (content, ev) => {
      if (selectedPatternId.value === 'reflexion') {
        // Reflexion 模式：Generator 为 call() 一次性调用（.call().content()），
        // 不产生流式 ReasoningEvent。若收到则忽略，避免与 ReflexionAttemptEvent 重复
        return
      }
      reasoningText.value += content
      if (selectedPatternId.value !== 'cot') {
        agentEvents.value = [...agentEvents.value, ev]
      }
      status.value = 'thinking'
    },
    ```
    4. 保持所有其他回调（onTotNode/onTotPrune/onFinal/onError 等）不变。
  </action>
  <verify>
    <automated>cd /Volumes/D/dev/workspace/AI/agents/frontend && npx vue-tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - submit() 的 startSSEStream options 含 onReflexionAttempt/onReflexionEvaluate/onReflexionReflect 三个回调，均追加到 agentEvents.value
    - onReasoning 回调在 selectedPatternId === 'reflexion' 时直接 return（不追加 reasoningText、不追加 agentEvents）
    - vue-tsc --noEmit 零错误
  </acceptance_criteria>
  <done>App.vue 含 onReflexionAttempt/onReflexionEvaluate/onReflexionReflect 回调且追加 agentEvents；reflexion 模式下 onReasoning 直接 return；vue-tsc 零错误。</done>
</task>

<task type="auto">
  <name>Task 3: Enable reflexion mode + fill patternDetails + PatternDescriptionCard title</name>
  <files>
    frontend/src/components/PatternSelector.vue
    frontend/src/components/PatternDescriptionCard.vue
    frontend/src/constants/patternDetails.ts
  </files>
  <read_first>
    frontend/src/components/PatternSelector.vue
    frontend/src/components/PatternDescriptionCard.vue
    frontend/src/constants/patternDetails.ts
  </read_first>
  <action>
    **PatternSelector.vue (per UI-SPEC):**

    更新 `isPatternEnabled()`，在 tot 条件之后添加：
    ```typescript
    function isPatternEnabled(patternId: string): boolean {
      return patternId === 'cot'
        || patternId === 'react'
        || patternId === 'selfAsk'
        || patternId === 'planExecute'
        || patternId === 'tot'
        || patternId === 'reflexion'  // Phase 9 新增
    }
    ```
    无其他改动 -- PHASE_ONLINE_MAP 已有 `reflexion: 9`，模板自动将 reflexion 从 "Phase 9 上线"（amber）切换为 "已上线"（绿色 tag）。roleplay 保持 disabled（"Phase 10 上线"）。

    **PatternDescriptionCard.vue (per UI-SPEC):**

    card-title 表达式顶部（tot 分支之后）添加 reflexion 分支：
    ```vue
    <div class="card-title">{{
        patternId === 'tot' ? 'Tree of Thoughts 树状思维' :
        patternId === 'reflexion' ? 'Reflexion 反思迭代' :
        patternId === 'planExecute' ? 'Plan-and-Execute 计划与执行' :
        patternId === 'selfAsk' ? 'Self-Ask 自问自答' :
        patternId === 'react' ? 'ReAct 推理+行动' :
        patternId === 'cot' ? 'CoT 思维链' : patternId
      }}</div>
    ```

    **patternDetails.ts (per 09-UI-SPEC §patternDetails.ts Update -- 文案逐字采用 UI-SPEC copywriting contract)：**

    替换空的 reflexion 条目（仅 coreIdea + scenarios，与现有 PatternDetail 接口一致）：
    ```typescript
    reflexion: {
      coreIdea: 'Reflexion（反思迭代）是一种让模型通过"尝试->评估->反思->改进"循环来自我纠错的 Agent 模式。'
        + 'Generator 生成初始答案，Evaluator（LLM-as-judge）评估答案质量并给出分数和反馈，'
        + 'Reflector 根据反馈生成反思。下一轮 Generator 依据反思内容改进答案，'
        + '直到评估通过或达到最大轮次。'
        + '整个过程就像一个学习者不断检查自己的作业并改进——"犯错不可怕，关键是要从错误中学习"。',
      scenarios: '需要迭代改进的创作任务（如"写一段代码然后优化"、"撰写文案并改进"）、'
        + '需要多轮打磨的推理问题（如"证明一个数学定理"）、'
        + '需要自我纠错的复杂任务（如"设计一个算法并分析其复杂度"）、'
        + '从差到好逐步改进的开放性问题（如"为一款新产品撰写宣传文案"）。',
    },
    ```
    其余条目（cot/react/selfAsk/planExecute/tot/roleplay）保持不变。
  </action>
  <verify>
    <automated>cd /Volumes/D/dev/workspace/AI/agents/frontend && npx vue-tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - PatternSelector.isPatternEnabled 对 'reflexion' 返回 true；reflexion 项模板显示绿色 "已上线" tag；roleplay 保持 disabled amber "Phase 10 上线"
    - PatternDescriptionCard 对 patternId==='reflexion' 显示标题 "Reflexion 反思迭代"
    - patternDetails.ts 的 reflexion 条目 coreIdea 和 scenarios 均为非空字符串（逐字采用 UI-SPEC copywriting contract）
    - vue-tsc --noEmit 零错误
  </acceptance_criteria>
  <done>PatternSelector 启用 reflexion（绿色已上线）；PatternDescriptionCard 显示 Reflexion 反思迭代；patternDetails reflexion 条目已填充；vue-tsc 零错误。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE event data -> TypeScript parser | 事件数据以 JSON 到达前端，断言为 Reflexion 3 类事件类型 |
| Event data -> Vue template | ReflexionAttemptEvent.answer / ReflexionEvaluateEvent.feedback / ReflexionReflectEvent.reflection 经 Vue 插值渲染 |
| SSE 事件名路由 | 未映射事件名静默忽略（前向兼容） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | 事件类型路由 | mitigate | 回调可选链 `?.`；路由边界处 `data as Reflexion*Event` 类型断言；未映射事件名静默忽略（前向兼容，per 02-UI-SPEC §Claude Discretion 决策 2） |
| T-09-07 | Tampering | SSE 事件数据 -> Vue 插值渲染 | accept | 无新 npm 依赖；answer/feedback/reflection 全部经 Vue 文本插值 `{{ }}` 渲染（无 v-html），与 Phase 5/6/7/8 一致 |
| T-09-08 | Denial of Service | 事件列表无界增长 | accept | 每次 submit 清空 agentEvents（既有 D-04 行为）；Reflexion 最多 2 轮 * 3 事件 = 6 个事件 + FinalAnswer，列表有界 |
| T-09-SC | Tampering | 无新增 npm 包 | mitigate | slopcheck: 验证 frontend/package.json 未变（无新前端依赖） |
</threat_model>

<verification>
- vue-tsc --noEmit 通过零错误（全部 7 个文件类型安全）
- ReflexionAttemptEvent/ReflexionEvaluateEvent/ReflexionReflectEvent TS 接口字段与后端 record 一一对应
- useSSEStream 路由 3 个 Reflexion 事件到 onReflexion* 回调
- App.vue 3 个回调将事件追加到 agentEvents；reflexion 模式下 onReasoning 忽略 ReasoningEvent
- PatternSelector reflexion 已上线（绿色），roleplay 保持 disabled
- patternDetails reflexion 条目完整填充
- 手动验证（需 DEEPSEEK_API_KEY）：启动前后端，选择 Reflexion 模式，提交问题，DevTools EventStream 确认 ReflexionAttemptEvent/ReflexionEvaluateEvent/ReflexionReflectEvent 依次到达，App.vue 回调正确追加事件
</verification>

<success_criteria>
1. 前端类型系统完整镜像后端 15 个 AgentEvent 类型（12 现有 + 3 Reflexion）[UI-11 类型部分]
2. SSE 路由将 3 个 Reflexion 事件正确送达回调 [UI-11 路由部分]
3. App.vue 在 reflexion 模式下不产生 reasoningText 重复内容 [反模式规避]
4. PatternSelector 显示 reflexion "已上线"（绿色）可选中
5. PatternDescriptionCard 显示 "Reflexion 反思迭代" 标题 + 核心思想 + 适用场景
6. vue-tsc 零错误，不破坏 Phase 5/6/7/8 现有事件路由
</success_criteria>

<output>
Create `.planning/phases/09-reflexion-pattern/09-02-SUMMARY.md` when done
</output>