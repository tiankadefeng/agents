---
phase: 09-reflexion-pattern
plan: 03
type: execute
wave: 2
depends_on:
  - 09-02
files_modified:
  - frontend/src/components/ReasoningPanel.vue
autonomous: true
requirements: [UI-11]
user_setup: []

must_haves:
  truths:
    - "ReasoningPanel detects reflexion mode via selectedPattern==='reflexion' and events containing ReflexionAttemptEvent"
    - "Reflexion round-grouping renders round dividers ('Round 1', 'Round 2') with dashed separator lines (per UI-SPEC)"
    - "Each round shows Attempt card (blue dot + label), Evaluate card (amber dot + score badge + feedback + pass/fail label), Reflect card (purple dot + reflection text) (per UI-SPEC)"
    - "Score badge uses 3-tier colors: >=8 green #15AC0C, 4-7 amber #FAB215, 1-3 red #D70016 (per UI-SPEC)"
    - "Pass/fail label shows '评估通过 (Passed)' green badge for score >= 8, '评估不通过，继续改进' for mid scores, '评估不通过 (Failed)' for last round low scores (per UI-SPEC)"
    - "Non-reflexion modes fall back to existing vertical timeline (preserving Phase 5/6/7/8 rendering)"
    - "ToT tree mode still renders independently (not affected by reflexion changes)"
  artifacts:
    - path: frontend/src/components/ReasoningPanel.vue
      provides: Reflexion round-grouping timeline rendering
      min_lines: 500
  key_links:
    - from: ReasoningPanel.vue
      to: types/agent.ts
      via: import of ReflexionAttemptEvent, ReflexionEvaluateEvent, ReflexionReflectEvent
      pattern: "import.*Reflexion(Attempt|Evaluate|Reflect)Event"
---

<objective>
实现 Reflexion 模式前端 ReasoningPanel 轮次分组渲染：新增 isReflexionMode 计算属性和 reflexionRounds 分组逻辑，渲染 round 分隔线 + 按轮分组的事件卡片（Attempt 蓝色/Evaluate 琥珀色/Reflect 紫色），评分徽章三段配色（绿色/琥珀色/红色），通过/不通过状态徽章。

Purpose: 用户选择 Reflexion 模式后，在 ReasoningPanel 看到"尝试->评估->反思"的完整循环过程——每轮用分隔线分组，Attempt 展示答案内容，Evaluate 展示评分和反馈，Reflect 展示反思内容。评分冠军3段可视色，通过状态清晰可见。

Output:
- ReasoningPanel.vue（改造：新增 isReflexionMode 渲染分支，round 分组逻辑，CSS 样式）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-reflexion-pattern/09-CONTEXT.md
@.planning/phases/09-reflexion-pattern/09-UI-SPEC.md
@.planning/phases/09-reflexion-pattern/09-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From frontend/src/components/ReasoningPanel.vue (current template structure):
```vue
<template>
  <div class="reasoning-panel">
    <el-collapse v-model="activeNames" @change="handleChange">
      <el-collapse-item name="reasoning">
        <template #title>...</template>

        <!-- Legacy text -->
        <template v-if="!events || events.length === 0">
          <pre v-if="reasoningText" class="reasoning-content">{{ reasoningText }}</pre>
          <div v-else class="empty-state">...</div>
        </template>

        <!-- Phase 8: ToT tree -->
        <div v-else-if="isTotMode" class="tot-tree-container">
          <div v-for="level in totLevels" :key="level" class="tot-level">...</div>
        </div>
        <!-- Optimal path legend -->
        <div v-if="isTotMode && props.totTree!.getOptimalPath().length > 0" class="tot-optimal-legend">...</div>

        <!-- Phase 5: vertical timeline -->
        <div v-else class="timeline">...</div>
      </el-collapse-item>
    </el-collapse>
  </div>
</template>
```

Current props:
```typescript
const props = defineProps<{
  reasoningText: string
  status: StreamStatus
  events?: AgentEvent[]
  totTree?: TotTree
  selectedPattern?: string
}>()
```

Current computed properties and functions:
- `isTotMode` — detects ToT mode
- `totLevels` — extracts level list from tree
- `isOptimalPath(nodeId)` — checks if node is on optimal path
- `scoreClass(score)` — 3-tier score coloring (>=7 gold, 4-6 amber, <=3 red)
- `isReplan(index)` — checks for replan divider

Note: The current `scoreClass` function uses ToT thresholds (>=7 gold). For Reflexion, the UI-SPEC defines different thresholds (>=8 green, 4-7 amber, 1-3 red). A separate `reflexionScoreClass` function is needed.

Event duck-typing pattern used in timeline:
```vue
<!-- ReasoningEvent -->
<div v-if="'content' in ev && !('toolName' in ev) && !('message' in ev) && !('steps' in ev)">...</div>
<!-- ToolCallEvent -->
<div v-else-if="'toolName' in ev && 'arguments' in ev">...</div>
```

Reflexion event duck-typing discriminators:
- ReflexionAttemptEvent: `'answer' in ev && !('score' in ev) && !('reflection' in ev)` (has answer, not score, not reflection)
- ReflexionEvaluateEvent: `'score' in ev && 'feedback' in ev` (has score AND feedback)
- ReflexionReflectEvent: `'reflection' in ev` (has reflection)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reflexion computed properties and helper functions to ReasoningPanel</name>
  <files>
    frontend/src/components/ReasoningPanel.vue
  </files>
  <read_first>
    frontend/src/components/ReasoningPanel.vue
    frontend/src/types/agent.ts
  </read_first>
  <action>
    **Script section changes:**

    1. **Import 扩展** — 在现有 import 中补充 Reflexion 事件类型：
    ```typescript
    import type { AgentEvent, ReasoningEvent, ToolCallEvent, ToolResultEvent, SubQuestionEvent, SubAnswerEvent, PlanEvent, StepStartEvent, StepCompleteEvent, ReflexionAttemptEvent, ReflexionEvaluateEvent, ReflexionReflectEvent } from '@/types/agent'
    ```

    2. **在 `isTotMode` computed 之后添加 Reflexion 计算属性：**

    ```typescript
    // Phase 9: Reflexion round 分组渲染模式 -- 选中 reflexion 且事件列表包含 ReflexionAttemptEvent
    const isReflexionMode = computed(() =>
      props.selectedPattern === 'reflexion'
      && !!props.events
      && props.events.some(ev => 'round' in ev && 'answer' in ev && !('score' in ev) && !('reflection' in ev))
    )

    // Phase 9: 提取所有 round 号，去重排序
    const reflexionRounds = computed(() => {
      if (!isReflexionMode.value || !props.events) return []
      const rounds = new Set<number>()
      for (const ev of props.events) {
        if ('round' in ev) {
          rounds.add((ev as any).round as number)
        }
      }
      return Array.from(rounds).sort()
    })

    // Phase 9: 获取指定 round 的所有事件
    function getEventsByRound(round: number): AgentEvent[] {
      return props.events?.filter(ev => 'round' in ev && (ev as any).round === round) ?? []
    }

    // Phase 9: Reflexion 评分三段配色 (8-10 pass / 4-7 mid / 1-3 low)
    // 独立于 ToT 的 scoreClass，因为阈值不同（ToT >=7 金，Reflexion >=8 绿）
    function reflexionScoreClass(score: number): string {
      if (score >= 8) return 'high'
      if (score >= 4) return 'mid'
      return 'low'
    }

    function isPassed(score: number): boolean {
      return score >= 8
    }

    function isLastRound(round: number): boolean {
      return reflexionRounds.value.length > 0
        && round === reflexionRounds.value[reflexionRounds.value.length - 1]
    }
    ```

    3. **保持全部现有 ToT 计算属性和函数不变。** 不修改 isTotMode、totLevels、isOptimalPath、scoreClass、isReplan。
  </action>
  <verify>
    <automated>cd /Volumes/D/dev/workspace/AI/agents/frontend && npx vue-tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - isReflexionMode computed 在 selectedPattern==='reflexion' 且 events 包含 ReflexionAttemptEvent 时返回 true
    - reflexionRounds computed 返回事件中所有 round 号去重排序的数组
    - getEventsByRound(round) 返回指定 round 的所有事件
    - reflexionScoreClass(score) 正确将 score 映射到 'high'(>=8)/'mid'(4-7)/'low'(1-3)
    - isPassed(score) 在 score >= 8 时返回 true
    - isLastRound(round) 在 round 是最后一轮时返回 true
    - 所有现有 ToT 计算属性（isTotMode/totLevels/isOptimalPath/scoreClass）不变
    - vue-tsc --noEmit 零错误
  </acceptance_criteria>
  <done>isReflexionMode/reflexionRounds/getEventsByRound/reflexionScoreClass/isPassed/isLastRound 函数存在且逻辑正确；现有 ToT 属性不变；vue-tsc 零错误。</done>
</task>

<task type="auto">
  <name>Task 2: Add reflexion round-grouping template + CSS to ReasoningPanel</name>
  <files>
    frontend/src/components/ReasoningPanel.vue
  </files>
  <read_first>
    frontend/src/components/ReasoningPanel.vue
  </read_first>
  <action>
    **模板结构调整（per UI-SPEC §ReasoningPanel 轮次分组渲染）：**

    当前模板结构为：
    ```
    <template v-if="!events || events.length === 0">...legacy text...</template>
    <div v-else-if="isTotMode" class="tot-tree-container">...</div>
    <div v-if="isTotMode && ..." class="tot-optimal-legend">...</div>
    <div v-else class="timeline">...</div>
    ```

    需要重构为：
    ```
    <template v-if="!events || events.length === 0">...legacy text...</template>
    
    <!-- Phase 8: ToT 树状层-列布局 -->
    <template v-else-if="isTotMode">
      <div class="tot-tree-container">...</div>
      <div v-if="props.totTree!.getOptimalPath().length > 0" class="tot-optimal-legend">...</div>
    </template>
    
    <!-- Phase 9: Reflexion round 分组布局 -->
    <template v-else-if="isReflexionMode">
      <div class="reflexion-container">
        <template v-for="(round, ri) in reflexionRounds" :key="round">
          <!-- 轮次分隔线 -->
          <div class="round-divider">
            <span class="round-divider-text">Round {{ round }}</span>
          </div>
    
          <!-- 该轮的所有事件 -->
          <div class="round-group">
            <template v-for="(ev, i) in getEventsByRound(round)" :key="i">
              <!-- ReflexionAttemptEvent -->
              <div v-if="'answer' in ev && !('score' in ev) && !('reflection' in ev)" class="timeline-item reflexion">
                <span class="dot reflexion-attempt"></span>
                <div class="label reflexion-attempt">尝试 (Attempt {{ round }})</div>
                <div class="content">{{ (ev as ReflexionAttemptEvent).answer }}</div>
              </div>
              <!-- ReflexionEvaluateEvent -->
              <div v-else-if="'score' in ev && 'feedback' in ev" class="timeline-item reflexion">
                <span class="dot reflexion-evaluate"></span>
                <div class="label reflexion-evaluate">评估 (Evaluate)</div>
                <div class="evaluate-content">
                  <div class="evaluate-score" :class="reflexionScoreClass((ev as ReflexionEvaluateEvent).score)">
                    评分：{{ (ev as ReflexionEvaluateEvent).score }}/10
                  </div>
                  <div class="evaluate-feedback">{{ (ev as ReflexionEvaluateEvent).feedback }}</div>
                  <div class="evaluate-passed" v-if="isPassed((ev as ReflexionEvaluateEvent).score)">
                    评估通过 (Passed)
                  </div>
                  <div class="evaluate-failed" v-else-if="isLastRound(round)">
                    评估不通过 (Failed)
                  </div>
                  <div class="evaluate-failed" v-else>
                    评估不通过，继续改进
                  </div>
                </div>
              </div>
              <!-- ReflexionReflectEvent -->
              <div v-else-if="'reflection' in ev" class="timeline-item reflexion">
                <span class="dot reflexion-reflect"></span>
                <div class="label reflexion-reflect">反思 (Reflect)</div>
                <div class="content">{{ (ev as ReflexionReflectEvent).reflection }}</div>
              </div>
            </template>
          </div>
        </template>
      </div>
    </template>
    
    <!-- Phase 5: vertical timeline (default) -->
    <template v-else>
      <div class="timeline">...</div>
    </template>
    ```

    具体改动要点：
    1. 将 `v-else-if="isTotMode"` 的 `<div>` 改为 `<template>`，将 `tot-tree-container` 和 `tot-optimal-legend` 两个 div 包裹在 `<template v-else-if="isTotMode">` 内。
    2. 在 ToT 模板之后、默认 timeline 之前插入 Reflexion 模板（`<template v-else-if="isReflexionMode">`）。
    3. 将默认 timeline 的 `<div v-else>` 改为 `<template v-else>`（包裹原 `<div class="timeline">`）。

    **CSS 样式（追加到 `<style scoped>` 尾部，per UI-SPEC §轮次分组 CSS 规格）：**

    ```css
    /* Phase 9: Reflexion round 分组布局 */
    .reflexion-container {
      padding: 12px 0;
    }
    .round-divider {
      display: flex;
      align-items: center;
      margin: 24px 0 16px;
      padding-left: 0;
    }
    .round-divider::before,
    .round-divider::after {
      content: '';
      flex: 1;
      height: 1px;
      border-top: 1px dashed #E4E7ED;
    }
    .round-divider-text {
      font-size: 12px;
      font-weight: 600;
      color: #909399;
      padding: 0 12px;
      white-space: nowrap;
    }
    .round-group {
      margin-bottom: 24px;
      padding-left: 24px;
    }
    .round-group:last-child {
      margin-bottom: 0;
    }
    .timeline-item.reflexion {
      position: relative;
      padding-left: 24px;
      margin-bottom: 16px;
    }
    .timeline-item.reflexion:last-child {
      margin-bottom: 0;
    }
    .dot.reflexion-attempt { border-color: #1D70F5; }
    .dot.reflexion-evaluate { border-color: #FAB215; }
    .dot.reflexion-reflect { border-color: #8B5CF6; }
    .label.reflexion-attempt { color: #1D70F5; font-size: 12px; font-weight: 600; margin-bottom: 4px; }
    .label.reflexion-evaluate { color: #FAB215; font-size: 12px; font-weight: 600; margin-bottom: 4px; }
    .label.reflexion-reflect { color: #8B5CF6; font-size: 12px; font-weight: 600; margin-bottom: 4px; }
    .evaluate-content {
      border: 1px solid #E4E7ED;
      border-radius: 4px;
      padding: 12px;
      margin-top: 8px;
    }
    .evaluate-score { font-size: 14px; font-weight: 600; margin-bottom: 8px; }
    .evaluate-score.high { color: #15AC0C; }
    .evaluate-score.mid { color: #FAB215; }
    .evaluate-score.low { color: #D70016; }
    .evaluate-feedback { font-size: 14px; color: #555555; line-height: 1.5; white-space: pre-wrap; word-wrap: break-word; }
    .evaluate-passed { font-size: 12px; font-weight: 600; margin-top: 8px; padding: 2px 8px; border-radius: 3px; display: inline-block; background: #E3F6E1; color: #15AC0C; }
    .evaluate-failed { font-size: 12px; font-weight: 600; margin-top: 8px; padding: 2px 8px; border-radius: 3px; display: inline-block; background: #FCE5E7; color: #D70016; }
    ```

    **保持全部现有 Phase 5/6/7/8 渲染逻辑不变。** 不修改时间线内部的 duck-typing 分支（ToT 模式下走树状分支，Reflexion 模式下走 round 分组，其他模式走垂直时间线——三者互斥）。不修改 `isTotMode` 或 `totLevels` 等现有逻辑。

    **空状态文案：** reflexion 模式树为空时（isReflexionMode=false 且 events 含 Reflexion 事件前的瞬间）沿用现有通用空状态，无需特判。
  </action>
  <verify>
    <automated>cd /Volumes/D/dev/workspace/AI/agents/frontend && npx vue-tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 模板结构中 ToT 树（含 legend）被 `<template v-else-if="isTotMode">` 包裹
    - Reflexion 轮次分组被 `<template v-else-if="isReflexionMode">` 包裹，在 ToT 之后、默认 timeline 之前
    - 默认垂直时间线被 `<template v-else>` 包裹（原 `<div v-else>`）
    - Reflexion 模板渲染 round 分隔线（虚线 + "Round N" 标签）、Attempt 卡片（蓝色 dot + 标签 + answer）、Evaluate 卡片（琥珀色 dot + 评分三段配色 + 反馈 + 通过/不通过徽章）、Reflect 卡片（紫色 dot + 反思内容）
    - 非 reflexion 模式回退现有垂直时间线或 ToT 树状布局，不破坏 Phase 5/6/7/8 渲染
    - CSS 样式包含所有指定颜色、间距、字体规格
    - vue-tsc --noEmit 零错误
  </acceptance_criteria>
  <done>模板含 round 分隔线 + Attempt/Evaluate/Reflect 分组卡片；评分三段配色 + 通过/不通过徽章；非 reflexion 模式回退现有渲染；vue-tsc 零错误。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Event data -> Vue template | ReflexionAttemptEvent.answer / ReflexionEvaluateEvent.feedback / ReflexionReflectEvent.reflection 经 Vue 插值渲染 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Tampering | Reflexion 事件数据 -> Vue 模板渲染 | accept | 所有内容经 Vue 文本插值 `{{ }}` 渲染（无 v-html），与 Phase 5/6/7/8 一致 |
| T-09-10 | Tampering | 事件判别 duck-typing | mitigate | 使用 3 个明确的条件分支（'answer' in ev / 'score' in ev / 'reflection' in ev），互斥判别，不匹配静默忽略 |
| T-09-11 | Tampering | 评分显示异常值 | accept | clamp(1,10) 在后端已保证；前端 reflexionScoreClass 对 1-10 外的值降级为 'low'（CSS 红色） |
| T-09-SC | Tampering | 无新增 npm 包 | mitigate | slopcheck: 验证 frontend/package.json 未变（无新前端依赖） |
</threat_model>

<verification>
- vue-tsc --noEmit 通过零错误
- isReflexionMode 在 selectedPattern==='reflexion' 且包含 ReflexionAttemptEvent 时启用
- reflexionRounds 从 events 中提取去重排序的 round 号
- 模板渲染顺序：Round 1 分隔线 -> Attempt 卡片 -> Evaluate 卡片（评分 + 反馈 + 通过/不通过） -> (可选) Reflect 卡片 -> Round 2 分隔线 -> ... -> 默认 timeline 回退
- 非 reflexion 模式回退到 ToT 树状布局或垂直时间线，不破坏现有渲染
- 手动验证（需 DEEPSEEK_API_KEY + 后端 Plan 01 完成）：启动前后端，选择 Reflexion 模式，提交"证明 √2 是无理数"，展开推理过程，确认 round 分组布局正确渲染 Attempt/Evaluate/Reflect 三个阶段
</verification>

<success_criteria>
1. Reflexion 轮次分组布局正确渲染 round 分隔线 + 按轮分组的事件卡片 [UI-11 渲染部分]
2. Attempt 卡片显示蓝色 dot + "尝试 (Attempt N)" 标签 + 答案内容
3. Evaluate 卡片显示琥珀色 dot + "评估 (Evaluate)" 标签 + 评分三段配色（绿色/琥珀色/红色）+ 反馈 + 通过/不通过徽章
4. Reflect 卡片显示紫色 dot + "反思 (Reflect)" 标签 + 反思内容
5. ToT 树状布局和垂直时间线不受影响，模式切换正确互斥
6. vue-tsc 零错误，不破坏 Phase 5/6/7/8 现有渲染
</success_criteria>

<output>
Create `.planning/phases/09-reflexion-pattern/09-03-SUMMARY.md` when done
</output>